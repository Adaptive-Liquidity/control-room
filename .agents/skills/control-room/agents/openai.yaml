interface:
  display_name: "Control Room"
  short_description: "Repo-specific patterns and workflows for control-room"
  default_prompt: "Use the control-room repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true